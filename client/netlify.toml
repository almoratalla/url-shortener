[build]
  publish = "dist"
  command = "npm install && npm run build:production"
  # This matches what's in the Netlify UI

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
